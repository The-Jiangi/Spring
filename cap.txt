git commit -a -m"修复查询学生反馈的bug；自动评价时删除对应活动的所有反馈"
完：
   修复查询学生反馈的bug；自动评价时删除对应活动的所有反馈
急：
    (测试)。
等：
    耗材同步角色

ssh root@192.168.199.11 -p 22526（测试）
ssh root@114.55.255.190 -p 10022（正式）
ssh hanggaoqt@192.168.100.114   （资产正式）
mysql -hlocalhost -P3307 -uroot -p123456

上传代码
    docker-compose exec main bash -lc "cap staging deploy"
    docker-compose exec main bash -lc "cap production_hzqcgjjg deploy"
    docker-compose exec main bash -lc "cap staging puma:start"
    docker-compose exec main bash -lc "puma_error.log"
修改网址
    git remote set-url origin (git@e.coding.net:jimjin/comprehensive-assessment-s.git)
上传前端
    rsync
    scp index.js hanggaoqt@192.168.100.114:/home/hanggaoqt/apps/hgao-ydgc-server/app/shared/public/client
    scp -r -P 22526 ./dist_new/* root@192.168.199.11:/app/shared/public
    scp -r -P 62223 ./dist/* root@192.168.80.250:/home/uroot/app/consumables-ms/app/shared/public/frontend_admin
    nginx -s reload
183.129.202.5:10443

20220816083930
vi /usr/local/bundle/bin/bundle

bin/rails db:seed RAILS_ENV=production		# 默认是开发环境，要加这个进生产环境
bin/rails db:migrate RAILS_ENV=production
bin/rails console RAILS_ENV=production
RAILS_ENV=production bundle exec rails c
bin/rails db:backup RAILS_ENV=production		# 备份数据库
PrintMachine
printer_contents_list
tail -f log/production.log 实时查看日志

强制推送
    git push -u origin dev -f

